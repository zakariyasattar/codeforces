#include <bits/stdc++.h>

using namespace std;
using ll = long long;

struct Node {
    int val;
    Node* left;
    Node* right;

    Node(int v) : val(v) {
        left = nullptr;
        right = nullptr;
    }
};

struct Pair {
    int x;
    int y;
};

int main() {
    int T;
    cin >> T;

    while(T--) {
        int n;
        cin >> n;
    }

    return 0;
}